
pipeline{
    agent any
    environments{
        l = env.language    
    }
    stages{
        stage('check language'){
            steps{                
                echo env.l
                sh '''
                    chmod +x ./script
                    echo env.l
                    ./script env.l
                '''                              
            }
        }
    }
}
