
pipeline{
    agent any
    stages{
        stage('check language'){
            steps{
                sh '''
                    chmod +x ./script
                    echo env.language
                    ./script "${language}"
                '''                              
            }
        }
    }
}
