
pipeline{
    agent any
    stages{
        stage('check language'){
            steps{
                echo env.language
                sh '''
                    chmod +x ./script
                    ./script "${language}"
                '''                              
            }
        }
    }
}
