
pipeline{
    agent any
    stages{
        stage('check language'){
            steps{
                sh '''
                    chmod +x ./script
                    echo "${language}"
                    ./script "${language}"
                '''                              
            }
        }
    }
}
